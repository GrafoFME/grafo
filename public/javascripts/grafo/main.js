let scale;
let mode;
// r - aristas recientes
// v - vertices con más aristas
// k - mapa de k's
// e - mapa de edades



function setup() {
    createCanvas(window.innerWidth, window.innerHeight);
}

function draw() {
    background(155);
    let c1 = color(255, 0, 0);
    let c2 = color(0, 255, 0);
}

function windowResized() {
    resizeCanvas(window.innerWidth, window.innerHeight);
}
